# ===========================================
# CMS Configuration
# ===========================================
# Copy this file to .env and fill in your values

# Enable/disable CMS integration
VITE_CMS_ENABLED=false

# CMS API base URL (e.g., https://your-cms-api.com)
VITE_CMS_API_URL=

# ===========================================
# SECURITY NOTE
# ===========================================
# NEVER store API keys or secrets in frontend environment variables!
# All VITE_ prefixed variables are exposed to the browser.
# 
# For authentication, use one of these approaches:
# 1. Backend proxy that adds credentials server-side
# 2. HTTP-only cookies for session-based auth
# 3. Public endpoints that don't require authentication
#
# ===========================================
# Example CMS configurations:
# ===========================================

# For a custom backend with cookie auth:
# VITE_CMS_API_URL=http://localhost:3001

# For Strapi (public API):
# VITE_CMS_API_URL=http://localhost:1337

# For production:
# VITE_CMS_API_URL=https://api.triji.com
